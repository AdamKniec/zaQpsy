import { StyleSheet } from "react-native";

const Styles = StyleSheet.create({
  wrapper: {
    backgroundColor: "blue",
    marginLeft: 20,
    borderRadius: "50%",
    justifyContent: "center",
    alignItems: "center",
    width: 30,
    height: 30,
  },
  innerText: { color: "white", textAlign: "center" },
});

export default Styles;
